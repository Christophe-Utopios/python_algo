# import module math
from math import *

# formule d'un cone droit
# volume = (pi * rayon² * hauteur)/3

# pi = pi 
# r = int(input ("valeur du rayon r :"))
# h = int(input ("valeur de la hauteur h : "))
# v = (pi * r**2 * h)/3
# print(v)

pi = pi 
rayon = float(input ("valeur du rayon r :"))
hauteur = float(input ("valeur de la hauteur h : "))
volume = (pi * rayon**2 * hauteur)/3
print(volume)
